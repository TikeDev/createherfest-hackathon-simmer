# OpenAI API key — get one at https://platform.openai.com/api-keys
VITE_OPENAI_API_KEY=sk-...

# (Local dev only) Point /api/scrape-recipe at a deployed Vercel URL to bypass local runtime issues.
# Leave empty in production — the relative path is used automatically.
# VITE_API_BASE=https://your-app.vercel.app
