# workflow checklist phase-implementation browser-verification
{"title":"Phase Implementation Checklist - MANDATORY STEPS","steps":[{"phase":"BEFORE Writing Code","actions":["1. Check git log for recent fixes: git log --grep='<related-keyword>' --since='3 days ago'","2. If fixes exist, READ them: git show <commit-hash>","3. Check actual exports/APIs: grep '^export' <file>","4. Verify current vs deprecated patterns: grep -c 'pattern' <file>","5. Understand WHY the pattern exists (read commit messages)"]},{"phase":"WHILE Writing Code","actions":["1. Use str_replace or diff for surgical edits (never overwrite entire files)","2. Include context in str_replace to make matches unique","3. Verify TypeScript compiles after each change: pnpm exec tsc --noEmit","4. Check git diff to see ACTUAL changes: git diff <file>"]},{"phase":"AFTER Writing Code - MANDATORY","actions":["1. Check TypeScript: pnpm exec tsc --noEmit","2. Check git diff: git diff --stat","3. Navigate to the page in browser: Chromedevtools.navigatePage","4. Check console for errors: Chromedevtools.listConsoleMessages","5. Take screenshots: both light and dark mode if UI changes","6. Verify the feature works: click through, test interactions","7. Check accessibility if applicable"]}],"critical":"NEVER skip the browser verification step. Code that compiles is NOT the same as code that works.","timestamp":"2026-03-07T04:11:00Z"}

